import {StyleSheet} from 'react-native';

export const MainContainer = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#FFFFFF'
  }
});

export const LandingLoader = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center"
  }
});

export const UserCss = StyleSheet.create({
  container: {
    marginHorizontal: 16,
    paddingVertical: 16
  },
  cardContainer: {
    marginBottom: 10,
    backgroundColor: '#EFEFEF'
  }
});
